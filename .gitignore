

# ----------------------------------------------------------------------
# 1. DEPENDENCIES / BUILD ARTIFACTS
# ----------------------------------------------------------------------

# Node Package Manager (npm)
# Dependencies installed via npm command
/node_modules
# Yarn cache and generated lock files
.yarn/
.pnp.*

# Build/Dist/Output folders
# Compiled/processed files from your build tools (e.g., Gulp, Webpack, Nuxt build)
/dist
/build
/out
/public
/.nuxt/
/.output/
/src/compress.js
/src/assets/compressed/

# Logs and Crash Dumps
# Logs are machine-generated and unnecessary to track
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# V8 crash files
/v8-profiler
/testem/reports
/coverage

# ----------------------------------------------------------------------
# 2. CONFIGURATION / SENSITIVE DATA
# ----------------------------------------------------------------------

# Environment Variables (Crucial for security!)
# Contains API keys, database credentials, etc.
.env
.env.local
.env.*.local
.DS_Store
.env.development
.env.production
# IDE and Editor Files (Local settings for individuals)
# Visual Studio Code
.vscode/*
!/.vscode/settings.json # Optional: Keep shared settings.json
!/.vscode/tasks.json    # Optional: Keep shared tasks.json
!/.vscode/launch.json   # Optional: Keep shared launch.json
!/.vscode/extensions.json # Optional: Keep recommended extensions

# JetBrains IDEs (IntelliJ, WebStorm, etc.)
.idea/

# macOS / OS Files
.DS_Store
.Trashes
ehthumbs.db

# Windows / OS Files
Thumbs.db

# ----------------------------------------------------------------------
# 3. NUXT/VUE SPECIFIC (Based on your context)
# ----------------------------------------------------------------------

# Nuxt/Vue Dev Tools
.nuxt-devtools/
.vite-inspect/

# Nuxt 3 cache and output directories
.nuxt
.output
.playground